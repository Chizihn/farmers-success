import { UserCircle } from "lucide-react";
import React from "react";

const User = () => {
  return (
    <div>
      <button>
        <UserCircle size={30} />
      </button>
    </div>
  );
};

export default User;
